# Only used by the Vite dev server (`npm run dev`), which is an advanced/optional
# path. It runs the Vue app on its own port (5173), so it needs to know where the
# Python server is. The normal workshop flow does NOT use this - the Python
# server serves the prebuilt app on port 8000.
VITE_PY_SERVER=http://localhost:8000
