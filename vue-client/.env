# No config needed for the normal workshop: the Python server serves this built
# app, so the Vue app talks to its own origin automatically.
#
# Only the Vite dev server (`npm run dev`, advanced/optional) needs to know where
# the Python server is - that override lives in .env.development.
